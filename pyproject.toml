[project]
name = "extra"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
  "projects/backfill-mastodon",
  "projects/android-recovery-anim",
]

[tool.black]
line-length = 120
